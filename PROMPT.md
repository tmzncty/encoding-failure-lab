# Ready-to-run agent prompt

把下面这一段直接交给一个有仓库写权限的 coding agent：

> Open this repository and continue it as an implementation project, not a planning exercise. Read `AGENTS.md`, `README.md`, `docs/PRIOR_ART.md`, `ROADMAP.md`, and `IMPLEMENTATION_PLAN.md` first. Then inspect the current tree and git history, identify the earliest unfinished dependency-safe task in `IMPLEMENTATION_PLAN.md`, and implement it fully with tests. Continue through subsequent adjacent tasks while the repository remains healthy; do not stop merely to summarize, propose a roadmap, or ask for confirmation when the repository already defines the decision. Keep bytes/text domains explicit, keep core trace/loss logic independent from UI, reuse standards-compliant platform APIs or mature encoding libraries instead of maintaining character maps, and do not turn the project into a generic converter or one-click fixer. For claims depending on current standards/runtime behavior, verify authoritative sources and record a dated note under `research/`. Commit coherent checkpoints to the repository, run the full test suite and production build before each milestone commit, fix failures you introduce, update completed checkboxes/documentation when the implementation actually satisfies them, and keep moving until you reach a genuine blocker or there are no required unfinished tasks left. When finished for this run, report exactly what was implemented, tests/build results, commits made, remaining next task, and any real blocker.